# smart-fitness-recommender-system
Machine learning model that recommends workout styles based on user profile data  (Gender, Fitness Goal, BMI Category). Trained using RandomForestClassifier and  prepared for integration into a mobile fitness app (Flutter).
